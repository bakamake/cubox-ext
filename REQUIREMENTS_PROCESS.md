# 需求协作规范

版本：v1.0　日期：2026-09-10　适用范围：cubox-ext 及后续项目的需求提报、缺陷反馈、需求变更。

## 1. 目的

本规范用于减少需求交付中的返工与沟通轮次。依据 2026-09-10 前 cubox-ext 项目的协作记录制定，问题模式均来自实际迭代过程。

## 2. 问题分析

### 2.1 环境约束后置导致方案返工

**事实**：sync 文件夹命名方案经历 4 轮迭代（随机 hex → storage.sync 收敛 → 固定名 → 双轨命名），每轮涉及代码重构。

**根因**：初始需求未包含完整使用环境。实施方在信息不全的情况下按通用场景设计，后续补充"单 Cubox 账号、仅使用 Firefox 系浏览器、书签经 Firefox 账号多设备同步"后，方案需整体调整。

**规定**：环境约束属于需求本体，必须在提报时完整给出。

### 2.2 事实与推测混合导致定位路径偏移

**事实**：-2004 错误排查中，初始推测为"浏览器内核检查 API"，后经 curl 复现排除浏览器因素，确认为服务端问题。

**根因**：推测与观测数据未分离，实施方需先行排除推测再定位，增加无效轮次。

**规定**：事实与推测必须分开陈述，推测单独标注且不作为定位依据。

### 2.3 需求变更未显式声明

**事实**：已确认的命名方案在实施完成后被修改（Chrome 系保留 sync-hex），变更以疑问形式提出，实施方需额外确认变更意图。

**规定**：变更已确认需求时，必须显式声明"变更"并指明被废弃的方案。

## 3. 提报格式

### 3.1 新需求

```
环境：账号数量 / 浏览器及内核 / 设备数量及同步方式
需求：期望的行为
验收：完成标准（日志特征 / 数量 / 界面表现）
约束：必须有 / 最好有 / 无所谓
手动步骤：是否接受一次性人工操作（如手动改名、手动清理）
```

### 3.2 缺陷反馈

```
操作：变更发生前的具体操作序列
期望：预期行为
实际：控制台日志原文，未经删减
推测：（可选）单独列出，仅供参考
```

### 3.3 需求变更

```
变更：声明本条目废弃前序方案 <方案名>
新方案：<内容>
生效范围：<仅新需求 / 含存量逻辑>
```

## 4. 执行要求

1. 提报前对照第 3 节格式自检，缺项补齐后提交。
2. 缺陷反馈中的日志必须包含完整时间线与请求记录，禁止仅描述结论。
3. 实施方收到需求后，如发现环境信息缺失，应一次性列明全部缺失项，禁止逐条追问。

## 5. 附：已验证有效的协作约定

1. 控制台日志原始粘贴，不做二次加工。
2. 错误处理分支保持最小化，优先实测暴露问题后再补。
